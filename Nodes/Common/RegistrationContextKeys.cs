namespace MAS_BT.Nodes.Common
{
    public static class RegistrationContextKeys
    {
        /// <summary>
        /// Flag that gets set after a capability extraction attempt completes (even if no capabilities were found).
        /// </summary>
        public const string CapabilitiesExtractionComplete = "CapabilitiesExtractionComplete";
    }
}
